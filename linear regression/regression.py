"""  @author Victor I. Afolabi  A.I. Engineer & Software developer  javafolabi@gmail.com  Created on 25 August, 2017 @ 8:15 PM.  Copyright (c) 2017. victor. All rights reserved."""# Linear Regression with gradient descentimport numpy as npfrom matplotlib import pyplot as pltdef squared_error(data, m, b):    total_error = 0    for i, d in enumerate(data):        x = d[0]        y = d[1]        total_error += (y - (m * x + b)) ** 2    return float(total_error) / len(data)def train(data, m, b, learning_rate, num_iter):    return [0, 0]def plot_data(x, y):    plt.scatter(x, y)    plt.show()def start():    data = np.genfromtxt('data.csv')    init_m = 0    init_b = 0    learning_rate = 1e-3    num_iter = 1000    m, b = train(data, init_m, init_b, learning_rate, num_iter)    print('m = {:.2f} b = {:.2f}'.format(m, b))if __name__ == '__main__':    start()